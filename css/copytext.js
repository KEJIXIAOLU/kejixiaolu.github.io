//复制1代码
document.getElementById('copyButton1').addEventListener('click', function() {
    var copyText = document.getElementById("copyText1");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制2代码
document.getElementById('copyButton2').addEventListener('click', function() {
    var copyText = document.getElementById("copyText2");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制3代码
document.getElementById('copyButton3').addEventListener('click', function() {
    var copyText = document.getElementById("copyText3");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制4代码
document.getElementById('copyButton4').addEventListener('click', function() {
    var copyText = document.getElementById("copyText4");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制5代码
document.getElementById('copyButton5').addEventListener('click', function() {
    var copyText = document.getElementById("copyText5");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制6代码
document.getElementById('copyButton6').addEventListener('click', function() {
    var copyText = document.getElementById("copyText6");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制7代码
document.getElementById('copyButton7').addEventListener('click', function() {
    var copyText = document.getElementById("copyText7");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制8代码
document.getElementById('copyButton8').addEventListener('click', function() {
    var copyText = document.getElementById("copyText8");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制9代码
document.getElementById('copyButton9').addEventListener('click', function() {
    var copyText = document.getElementById("copyText9");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制10代码
document.getElementById('copyButton10').addEventListener('click', function() {
    var copyText = document.getElementById("copyText10");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制11代码
document.getElementById('copyButton11').addEventListener('click', function() {
    var copyText = document.getElementById("copyText11");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制12代码
document.getElementById('copyButton12').addEventListener('click', function() {
    var copyText = document.getElementById("copyText12");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制13代码
document.getElementById('copyButton13').addEventListener('click', function() {
    var copyText = document.getElementById("copyText13");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制14代码
document.getElementById('copyButton14').addEventListener('click', function() {
    var copyText = document.getElementById("copyText14");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制15代码
document.getElementById('copyButton15').addEventListener('click', function() {
    var copyText = document.getElementById("copyText15");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制16代码
document.getElementById('copyButton16').addEventListener('click', function() {
    var copyText = document.getElementById("copyText16");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制17代码
document.getElementById('copyButton17').addEventListener('click', function() {
    var copyText = document.getElementById("copyText17");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});


//复制18代码
document.getElementById('copyButton18').addEventListener('click', function() {
    var copyText = document.getElementById("copyText18");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制19代码
document.getElementById('copyButton19').addEventListener('click', function() {
    var copyText = document.getElementById("copyText19");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制20代码
document.getElementById('copyButton20').addEventListener('click', function() {
    var copyText = document.getElementById("copyText20");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制21代码
document.getElementById('copyButton21').addEventListener('click', function() {
    var copyText = document.getElementById("copyText21");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制22代码
document.getElementById('copyButton22').addEventListener('click', function() {
    var copyText = document.getElementById("copyText22");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制23代码
document.getElementById('copyButton23').addEventListener('click', function() {
    var copyText = document.getElementById("copyText23");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制24代码
document.getElementById('copyButton24').addEventListener('click', function() {
    var copyText = document.getElementById("copyText24");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制25代码
document.getElementById('copyButton25').addEventListener('click', function() {
    var copyText = document.getElementById("copyText25");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});

//复制26代码
document.getElementById('copyButton26').addEventListener('click', function() {
    var copyText = document.getElementById("copyText26");
    var selection = window.getSelection();
    var range = document.createRange();
    range.selectNodeContents(copyText);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges();
    //alert("文本已复制到剪贴板");
});


